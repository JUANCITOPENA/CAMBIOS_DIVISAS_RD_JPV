const express = require("express"); const dotenv = require("dotenv"); const fetch = require("node-fetch"); dotenv.config(); const app = express(); app.use(express.static("public")); app.get("/api/get-rates", async (req, res) => { const API_KEY = process.env.EXCHANGE_API_KEY; if (!API_KEY) return res.status(500).json({ error: "Llave no configurada en .env" }); try { const response = await fetch(`https://v6.exchangerate-api.com/v6/${API_KEY}/latest/DOP`); const data = await response.json(); res.json(data); } catch (e) { res.status(500).json({ error: e.message }); } }); app.listen(3000, () => console.log("✅ Servidor en http://localhost:3000"));
